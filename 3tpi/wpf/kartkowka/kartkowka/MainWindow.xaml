<Window x:Class="kartkowka.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kartkowka"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TextBox x:Name="txt1" Width="100" Height="20" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="50,0,0,0">
            <TextBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Kopiuj" Click="copy_click"></MenuItem>
                </ContextMenu>
            </TextBox.ContextMenu>
        </TextBox>
        <TextBox Name="txt2" Width="100" Height="20" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,50,0">
            <TextBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Click="wklej_click" Header="Wklej"></MenuItem>
                </ContextMenu>
            </TextBox.ContextMenu>
        </TextBox>
        <Button x:Name="btn1" Content="Przenies" Width="100" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center" Click="btn1_Click"/>
    </Grid>
</Window>
